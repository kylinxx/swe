# 演示项目：一个故意带 bug 的小计算器

这个目录专门给 coding agent 录视频演示使用。

任务建议：
1. 运行单元测试，观察失败。
2. 阅读 `calculator.py` 和 `test_calculator.py`。
3. 找到 `average()` 的计算错误并修复。
4. 重新运行测试，确保全部通过。

预期效果：
- `average([1, 2, 3]) == 2.0`
- `average([10]) == 10.0`
- `format_average([2, 4]) == "average=3.00"`

