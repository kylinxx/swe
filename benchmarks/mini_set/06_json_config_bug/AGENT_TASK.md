# Task

Fix `load_config()` so that JSON defaults and type handling are correct.

## Rules

- Keep the function simple and deterministic.
- Preserve explicit values from the JSON input.
- Make the empty input path return sensible defaults.
