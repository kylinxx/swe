# Task

Fix `build_summary()` so that passed and failed items are counted correctly.

## Rules

- Keep the returned structure stable.
- Success rate should be computed with the total number of results.
